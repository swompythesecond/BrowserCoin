# Loaded by `npm run dev:local` (vite --mode localhelpers).
# Points default helper lists at the local split servers (server:api on :9000,
# server:peerjs on :9001) instead of the live browsercoin.org network.
VITE_HELPERS=local
