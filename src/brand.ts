/**
 * User-facing brand constants. On-chain / network identifiers (PoW salt, peer
 * prefix, IndexedDB name, wallet-file `type`) intentionally keep their legacy
 * `wwwcoin` form: changing those would fork the chain and orphan every
 * existing wallet on the network.
 */
export const NAME = 'BrowserCoin';
export const TICKER = 'BROWSER';
export const TAGLINE = 'a cryptocurrency that lives in your browser';
